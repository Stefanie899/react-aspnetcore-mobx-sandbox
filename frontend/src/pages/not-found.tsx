import React from "react";

const NotFoundPage: React.FC = () => {
    return (
        <React.Fragment>
            <h1>Uhh, that page isn't a thing...</h1>
        </React.Fragment>
    );
}

export default NotFoundPage;